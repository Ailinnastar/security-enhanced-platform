{
  "name": "taskpilot-monorepo-root",
  "private": true,
  "description": "Run and install commands delegate to security-enhanced-platform/",
  "scripts": {
    "install": "npm --prefix security-enhanced-platform install && npm --prefix security-enhanced-platform run install:all",
    "start": "npm --prefix security-enhanced-platform start",
    "test": "npm --prefix security-enhanced-platform test"
  }
}
